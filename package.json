{
  "name": "rickandmortyapi",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",  // Start your application
    "test": "echo \"Error: no test specified\" && exit 1",
    "sequelize": "sequelize",  // Shortcut to run Sequelize CLI locally
    "db:migrate": "npm run sequelize -- db:migrate",  // Run migrations
    "db:migrate:undo": "npm run sequelize -- db:migrate:undo",  // Undo the last migration
    "db:migrate:undo:all": "npm run sequelize -- db:migrate:undo:all",  // Revert all migrations
    "db:seed:all": "npm run sequelize -- db:seed:all",  // Seed the database
    "db:seed:undo": "npm run sequelize -- db:seed:undo"  // Undo seed data
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "async-retry": "^1.3.3",
    "axios": "^1.7.2",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "path": "^0.12.7",
    "pg": "^8.12.0",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.37.3"
  }
}
